# Bloom & Stems Landing Page

A simple static landing page for Bloom & Stems, a local flower shop.

## Structure

- `index.html` — page markup
- `styles.css` — styling

## Running locally

Just open `index.html` in a browser, or serve the folder with any static file server:

```
npx serve .
```
